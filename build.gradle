
//构建脚本配置
buildscript {

    //构建脚本依赖仓库
    repositories {
        jcenter()
        maven { url 'https://jitpack.io' }
    }

    //构建脚本依赖
    dependencies {

    }

}

//工程配置
apply from: 'build-project.gradle'
apply from: 'build-upload.gradle'
